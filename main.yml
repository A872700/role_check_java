---
- name: check java installed on target machine
  tasks:
  - name: check java 
    shell: java -version
    register: version
  - name: check java with other command
    shell: command -v java
    register: version2
  - name: Debug module
    debug:
     msg: "This is first output {{ version }} This  is second output {{ version2 }}"
